{
  "name": "olbos",
  "private": true,
  "description": "Olbos — autonomous treasury engine for the x402 economy (Solana). Open-source SDK, MCP server, and CLI.",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
